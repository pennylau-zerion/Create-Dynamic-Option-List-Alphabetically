# Create-Dynamic-Option-List-Alphabetically

//Variables need to be changed

$servername = "XXX";
$profileID = "XXX"; 
$optionlistID = "XXX";
$optionlist_key = trim($recordInfo['XXX']);
$optionlist_label = trim($recordInfo['XXX']);


$client = 'XXXX';
$secret = 'XXX';
